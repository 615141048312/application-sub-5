<!DOCTYPE html>
<html>
  <head>
    <title>Bookers</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render '/layouts/header' %>
    <div class="main container">
      <div class="row">
        <% if flash[:notice] %>
          <div>
            <%= flash[:notice] %>
          </div>
        <% end %>
      </div>
      <div class="row">
        <% if user_signed_in? %>
        <%= form_tag search_path, method: :get do %>
          <%= text_field_tag :search %>
          <%= select_tag(:option, options_for_select([["ユーザー","user"],["投稿","book"]])) %>
          <%= select_tag(:way, ways_for_select([["完全一致","perfect"],["前方一致","front"],["後方一致","behind"],["部分一致","part"]])) %>
          <%= submit_tag "検索" %>
        <% end %>
        <% end %>
      </div>
      <div class="row">
        <%= yield %>
      </div>
    </div>
    <footer>
      <div class="container">
        <div class="row">
          <div class="col-xs-3 col-xs-offset-9">
            <p>CopyRight Infratop.inc</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
